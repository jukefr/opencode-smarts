---
description: Explore the codebase to answer a question or map a system
agent: explorer
subtask: true
---

Explore the codebase to answer this question:

$ARGUMENTS

Use `grep`, `glob`, `read`, and `lsp` to find relevant files and trace the code flow. Return a clear, structured answer with file references (file:line format).

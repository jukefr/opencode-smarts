# Project-Specific OpenCode Rules
## Project Overview
- Name: [Replace with your project name]
- Description: [Replace with short project description]
- Tech Stack: [Replace with e.g. TypeScript, React, Node.js]

## Project Commands
- Test: [Replace with e.g. `npm test`]
- Lint: [Replace with e.g. `npm run lint`]
- Build: [Replace with e.g. `npm run build`]
- Typecheck: [Replace with e.g. `npm run typecheck`]

## Architecture Notes
[Add key architecture decisions, important file paths, module structure here]
Example:
- `src/agent/` - Custom agent definitions
- `src/tool/` - Custom tool implementations
- Auth logic lives in `src/middleware/auth.ts`

## Module-Specific Context
Uncomment and modify these lines to import submodule-specific AGENTS.md files:
# @src/auth/AGENTS.md
# @src/components/AGENTS.md
# @src/api/AGENTS.md
